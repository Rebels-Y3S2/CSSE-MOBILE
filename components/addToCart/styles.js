import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
    amountView: {
        flexDirection: 'row',
        justifyContent: 'center'
    },
    amountText: {
        fontSize: 25,
        margin: 10
    },
    btn: {
        margin: 5
    },
    animation: {
        slideFrom: 'bottom'
    }
});